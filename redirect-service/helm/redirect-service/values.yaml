# Default values for redirect-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1

image:
  repository: redirect-service
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
